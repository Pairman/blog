---
import { BASE } from "../config.ts";
const { tag } = Astro.props;
---

<a
  href={BASE + "/tags/" + tag}
  class="not-prose relative z-10 text-xs font-medium text-base-500 hover:text-base-200 transition-colors duration-100"
>
  {tag}
</a>
